#include <stdio.h>
/* main: this program will prints the number of arguments passed into it.
 *
 * @argc: Thsi will print the number of argument
 * @argv: This will print the name of argument passed into it.
 *
 * Return (0);
 */

int main(int argc, char *argv[])
{	
		printf("%d", argc - 1);

	return 0;
}
